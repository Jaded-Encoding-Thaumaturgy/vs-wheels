name: Upload Release

on:
  workflow_call:
    inputs:
      wheel-pattern:
        description: Pattern for wheel artifacts
        type: string
        required: true
      sdist-name:
        description: Name for sdist artifact
        type: string
        required: true
      display-name:
        description: Name used for release tag and title (e.g., vapoursynth-fmtconv)
        type: string
        required: true
      upload-hf:
        description: Upload to Hugging Face bucket
        type: boolean
        required: false
        default: false

jobs:
  upload:
    name: Upload to GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Download wheels
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: ${{ inputs.wheel-pattern }}
          path: dist
          merge-multiple: true

      - name: Download sdist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: ${{ inputs.sdist-name }}
          path: dist

      - name: Determine version
        id: version
        run: |
          WHEEL=$(ls dist/*.whl | head -1)
          VERSION=$(basename "$WHEEL" | sed 's/^[^-]*-\([^-]*\)-.*/\1/')
          TAG="${{ inputs.display-name }}-v$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Detected version: $VERSION"
          echo "Release tag: $TAG"

      - name: Generate PEP 658 metadata
        run: python scripts/generate_metadata.py

      - name: Create release and upload assets
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ inputs.display-name }} ${{ steps.version.outputs.version }}
          files: dist/*
          body: ${{ inputs.display-name }} ${{ steps.version.outputs.version }}
          fail_on_unmatched_files: true
          generate_release_notes: false

  # upload-bucket:
  #   name: Upload to HuggingFace Bucket
  #   needs: upload
  #   if: ${{ inputs.upload-hf }}
  #   permissions:
  #     id-token: write
  #     contents: read
  #   uses: ./.github/workflows/cd-upload-bucket.yml
  #   with:
  #     wheel-pattern: ${{ inputs.wheel-pattern }}
  #     sdist-name: ${{ inputs.sdist-name }}

  trigger-index:
    name: Trigger Index Update
    # needs: [upload, upload-bucket]
    needs: [upload]
    if: ${{ always() && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Trigger index update
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: gh api repos/$GH_REPO/dispatches -f event_type=index
